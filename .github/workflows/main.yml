name: Ejecución Diaria

#on:
#  schedule:
#    - cron: "30 11,18 * * *"
on:
  push:
    branches:
      - master


jobs:
  ejecutar:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout del repositorio
      uses: actions/checkout@v2

    - name: Configurar Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.x

    - name: Instalar dependencias
      run: pip install -r requirements.txt

    - name: Ejecutar script Python
      run: python main.py
      env:
        EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        DRIVE_JSON: ${{ secrets.DRIVE_JSON }}
        #GITHUB_TOKEN2: ${{ secrets.NICO_TOKEN2 }}
        
    - name: Push CSV file
      run: |
          git config user.name "nicolas0715"
          git config user.email "oteronicolas3@gmail.com"
          git add productos.csv
          git commit -m "Agregando productos.csv"
          git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}