name: Ejecución Diaria

#on:
#  schedule:
#    - cron: "30 11,18 * * *"
on:
  push:
    branches:
      - master


jobs:
  ejecutar:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout del repositorio
      uses: actions/checkout@v2

    - name: Configurar Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.x

    - name: Instalar dependencias
      run: pip install -r requirements.txt

    - name: Ejecutar script Python
      run: python main.py

    - name: Imprimir archivo txt mediante Email
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.gmail.com.
        server_port: 587
        username: ${{ secrets.EMAIL_USERNAME }}
        password: ${{ secrets.EMAIL_PASSWORD }}
        subject: "Correo de prueba"
        body: "Este es un correo electrónico de prueba enviado desde GitHub Actions."
        from: oteronicolas3@gmail.com
        to: 72398140.241809@lon1.printnode.com 
        attachments: prod_lista0.txt